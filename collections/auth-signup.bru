meta {
  name: auth/signup
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/signup
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Admin User",
    "email": "admin@test.com",
    "password": "123456"
  }
}

script:post-response {
  bru.setEnvVar("userId", res.body.id)
}

settings {
  encodeUrl: true
  timeout: 0
}
